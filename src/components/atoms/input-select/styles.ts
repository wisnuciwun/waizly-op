import colors from '@/utils/colors';

const styles = {
    required: {
        color: colors.red
    },
    textSmall: {
        fontSize: 12
    },
    label: {
        fontWeight: 'bold'
    }
};

export default styles;